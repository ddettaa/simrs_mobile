import 'package:flutter/material.dart';

class PatientsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Patients CRUD Screen'),
    );
  }
}
